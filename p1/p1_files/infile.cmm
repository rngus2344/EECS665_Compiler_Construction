int var = 34;
1234S;
if(var > 10) { return true; }
"\n";
"use \" to  for a quote and \\ for a backslash";
"unterminated
"also unterminated \"
"backslash followed by space: \ is not allowed"
"bad escaped character: \a AND not terminated
# Comment here. // still comment. # and more.
